---
"kamaji": patch
---

Diary grid: most recent week now renders first (leftmost), duplicate month labels are suppressed, and narrow screens get a vertical layout with left-aligned month labels and full-width day cells.
